% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/api.R
\name{febama_config}
\alias{febama_config}
\title{Create a FEBAMA model configuration}
\usage{
febama_config(
  frequency = 12,
  ets_model = "AAN",
  ts_scale = TRUE,
  forecast_h = 18,
  train_h = 1,
  history_burn = 25,
  PI_level = 90,
  roll = NULL,
  feature_window = NULL,
  fore_model = c("ets_fore", "naive_fore", "rw_drift_fore", "auto.arima_fore"),
  features_used = c("x_acf1", "diff1_acf1", "entropy", "alpha", "beta", "unitroot_kpss"),
  lpd_features_parallel = FALSE,
  ncores = 1,
  variable_selection = TRUE,
  varsel_init = c("all-in", "all-out", "random"),
  beta_shrinkage = 10,
  init_optim = TRUE,
  alg_name = c("MAP", "sgld"),
  n_iter = 1,
  max_batch_size = 108,
  n_epoch = 10,
  burnin_prop = 0.4,
  stepsize = 0.1,
  sgld_gamma = 0.55,
  sgld_a = 0.4,
  sgld_b = 10
)
}
\arguments{
\item{frequency}{Seasonal frequency of the input series.}

\item{ets_model}{ETS model code kept for compatibility with older scripts.}

\item{ts_scale}{Should the time series be standardized before fitting base forecasters?}

\item{forecast_h}{Forecast horizon.}

\item{train_h}{Historical holdout horizon used when building training scores.}

\item{history_burn}{Minimum history length before rolling training starts.}

\item{PI_level}{Prediction interval level used to infer forecast standard deviations.}

\item{roll}{Optional rolling-history window for fitting base forecasters.}

\item{feature_window}{Optional rolling-history window for feature calculation.}

\item{fore_model}{Character vector of base forecaster function names.}

\item{features_used}{Character vector shared by each non-baseline model, or a
list with one character vector per non-baseline model.}

\item{lpd_features_parallel}{Should historical predictive densities and
features be computed in parallel?}

\item{ncores}{Number of worker cores to use when \code{lpd_features_parallel} is
\code{TRUE}.}

\item{variable_selection}{Should feature-selection indicators be sampled?}

\item{varsel_init}{Initial feature-selection state.}

\item{beta_shrinkage}{Diagonal covariance scale for the coefficient prior.}

\item{init_optim}{Should BFGS be used to initialize coefficients?}

\item{alg_name}{Inference algorithm, either \code{"MAP"} or \code{"sgld"}.}

\item{n_iter}{Number of outer MCMC iterations.}

\item{max_batch_size}{Maximum SGLD minibatch size.}

\item{n_epoch}{Number of SGLD epochs inside each outer update.}

\item{burnin_prop}{Within-SGLD burn-in proportion for Polyak averaging.}

\item{stepsize}{Fixed SGLD step size. Use \code{NA} for the decaying schedule.}

\item{sgld_gamma}{Exponent for the decaying SGLD step-size schedule.}

\item{sgld_a}{Scale for the decaying SGLD step-size schedule.}

\item{sgld_b}{Offset for the decaying SGLD step-size schedule.}
}
\value{
A list consumable by the FEBAMA training and forecasting functions.
}
\description{
This constructor keeps the package examples and scripts from hand-building
large nested configuration lists.
}
